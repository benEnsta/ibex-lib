language: cpp

compiler:
  - gcc
  - clang

before_script:
  - sudo apt-get update -q
  - sudo apt-get install -y libcppunit-dev
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - make test ARGS="-VV"
  - sudo make install
